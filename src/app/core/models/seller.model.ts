export interface Seller {
  id: number;
  photo: string;
  name: string;
  email: string;
  status: boolean;
}

export interface SellerDTO {
  photo: string;
  name: string;
  email: string;
  status: boolean;
}
